# 大模型系统学习与面试指南(进阶完整版)

> **定位**:一份帮助「系统理解大模型」并为「校招/社招面试」做好准备的完整指南。
> **覆盖**:基础概念 → 原理 → 训练对齐 → 推理部署 → 提示工程 → RAG → Agent → 生态应用 → 面试题库 → 学习路线。
> **时效**:行业信息更新至 **2026 年 8 月**，但型号、价格、参数和评测会快速变化；使用时应以对应厂商的官方模型卡、技术报告和产品文档为准。原理性内容相对稳定，但具体实现可能不同。
> **阅读建议**:想快速建立认知,先读第 1、10、11 章与附录速记卡;备战面试,重点啃第 2、4、6、8、9 章和第 12 章题库。

---

## 目录

- [第 1 章 大模型基础概念](#第-1-章-大模型基础概念)
- [第 2 章 Transformer 深度原理](#第-2-章-transformer-深度原理)
- [第 3 章 预训练:数据工程与训练全流程](#第-3-章-预训练数据工程与训练全流程)
- [第 4 章 后训练与对齐:RLHF · DPO · GRPO · 推理模型](#第-4-章-后训练与对齐rlhf--dpo--grpo--推理模型)
- [第 5 章 微调与参数高效微调(LoRA/QLoRA)](#第-5-章-微调与参数高效微调loraqlora)
- [第 6 章 推理与部署优化](#第-6-章-推理与部署优化)
- [第 7 章 提示词工程与上下文工程](#第-7-章-提示词工程与上下文工程)
- [第 8 章 检索增强生成 RAG 深度](#第-8-章-检索增强生成-rag-深度)
- [第 9 章 Agent 智能体与工具使用](#第-9-章-agent-智能体与工具使用)
- [第 10 章 模型生态与行业格局](#第-10-章-模型生态与行业格局)
- [第 11 章 最新应用场景与落地(2026)](#第-11-章-最新应用场景与落地2026)
- [第 12 章 面试题大全与深度追问](#第-12-章-面试题大全与深度追问)
- [第 13 章 学习路线图(细化到周)](#第-13-章-学习路线图细化到周)
- [第 14 章 资源推荐](#第-14-章-资源推荐)
- [第 15 章 面试避坑与加分项](#第-15-章-面试避坑与加分项)
- [附录 面试前速记卡](#附录-面试前速记卡)
- [修订记录与数据说明](#修订记录与数据说明)

---

# 第 1 章 大模型基础概念

## 1.1 什么是大语言模型(LLM)

**定义**:大语言模型(Large Language Model)是基于 Transformer 架构、在大规模文本语料上通过自监督方式预训练得到的深度神经网络模型,能够理解、推理并生成自然语言。

**与传统 NLP 的核心区别**(面试常问,回答要能展开):

| 维度 | 传统 NLP | 大模型 |
|------|----------|--------|
| 范式 | 一任务一模型(分类/分词/NER 各自训练) | 一个模型通吃多任务,通过指令/上下文切换 |
| 特征 | 手工特征 + 任务专用结构 | 全数据驱动,自学习表示 |
| 数据 | 标注数据(昂贵、有限) | 无标注数据(充足、廉价) |
| 能力 | 每任务需要重新训练 | 涌现出 In-Context Learning、推理、代码、对齐等能力 |
| 扩展 | 难以复用 | Scaling Law:算力/数据/参数增长 → 能力提升 |

**核心特征**:
1. **规模大**:参数量覆盖数十亿、数百亿到稀疏 MoE 的千亿乃至万亿级总参数。参数规模影响模型容量，但不单独决定实际能力。
2. **自监督预训练**:在大规模无标注数据上学习,核心目标函数是「预测下一个 Token」(Next Token Prediction)。
3. **涌现能力(Emergence)**:某些能力会在规模增大后表现出明显跃升，但是否存在清晰阈值、是否只是评测指标造成的表观跃迁仍有讨论，不能一概而论。
4. **通用与生成**:以 Decoder-only 架构统一「理解」与「生成」,输出是概率采样而不是模板拼接。
5. **可通过指令/上下文适配新任务**:不用改权重,给示例或指令即可现场完成任务(In-Context Learning)。

## 1.2 Token 与分词(Tokenizer)

**Token 是模型处理和计费的最小单位**。LLM 不直接读字符,而是把文本切分成 token 序列再转成 id 送入网络。

**主流分词算法**:
- **BPE(Byte Pair Encoding)**:从字符级开始,反复合并出现频率最高的相邻符号对,直到达到词表规模。本质是「子词」切分。GPT、Llama、多数开源模型使用。
- **SentencePiece**:把 BPE/Unigram 等方法做成统一框架,直接从原始文本(包括空格)学习并切分子词,不依赖外部预分词。多用于日语/韩语/多语言模型(Gemma 等)。
- **WordPiece**:BERT 使用,合并时以「能提升似然」为准则,与 BPE 略有不同。
- **Unigram**:基于语言模型概率消减候选子词,训练时从大词表逐步剪枝。

**中文切分特点**:中文常按字或子词切分(而非按词),所以「大模型」可能被切成「大」「模型」或「大模」「型」。**中文 1 个汉字约等于 1 个 token,英文 1 个单词约 1~2 个 token**(因词根/后缀拆分)。

**与工程相关的要点**(面试加分):
- Token 直接决定**费用、上下文容量、延迟**。同样内容,不同模型的分词效率不同(所以厂商会标「XX 万 tokens」上下文,而真实字符数取决于语言)。
- 子词 Tokenizer 通常能缓解词表外词(OOV)问题;罕见词、专有名词和代码标识符可能被拆成更多子词，从而增加 token 数和处理成本。

## 1.3 上下文窗口(Context Window)

**定义**:模型单次输入(提示 + 历史 + 正在生成的内容)能容纳的最大 token 数。

**当前水平(2026)**:不同厂商和型号的上下文窗口差异很大，公开产品中可见 128K、200K 乃至更长的配置；具体输入/输出上限、计费和长上下文效果必须以对应官方文档为准，不能把某个型号的能力推广到整个系列。

**超出窗口会发生什么**:
- **截断**:超过窗口时删除一部分历史，可能丢掉关键信息；保留开头、结尾还是最近内容取决于具体截断策略。
- **压缩**:摘要式压缩历史,信息有损。
- **检索**:只把相关内容(如 RAG 分块)放进去。
- 代价曲线:**成本随 token 线性增长,延迟随 token 增长,注意力计算开销随上下文长度超线性增长**(长文本更贵,这是「检索优于全量」的另一原因)。

**关键认知(面试高频)**:长窗口 ≠ 完全利用。
- 存在「**Lost in the Middle**」现象:模型最能准确利用开头与结尾的内容,对位于中间的段落利用最差。
- 上下文利用率随长度下降,信息容易「淹没」。
- 所以工程上,「检索 → 只给相关内容」往往比「全量塞入」效果更好、成本更低。

## 1.4 缩放定律(Scaling Laws)

**一句话**:模型能力主要由「参数量 N、训练数据量 D、计算量 C」决定,三者在某个预算下存在最优配比。

**三篇关键论文**:
- **Kaplan et al.(2020,OpenAI)**:模型性能与参数量呈**幂律关系**(log-log 几乎直线),提出「大模型 + 相对少的数据」的直觉。
- **Chinchilla(2022,DeepMind)**:在其模型、数据和计算预算假设下，指出**数据和参数都很重要**。原论文给出的计算最优趋势常被近似为训练 token 数约等于 **20 × 参数量**(即 70B 模型约 1.4T tokens);这不是所有模型、任务或训练阶段的固定配比。
- **Kaplan/Chinchilla 的意义**:解释「为什么要堆参数」「为什么要堆数据」「为什么要配比均衡」；在参数较大而数据不足时，模型可能处于训练不足(under-trained)状态，增加高质量数据有助于更充分利用参数和算力。

**训练预算公式(直觉版)**:C ≈ 6ND(N 为参数量,D 为训练 token 数;6 倍 ≈ 前向 2ND + 反向 4ND,优化器与激活另计)。这是面试中「估算训练一个模型要多少算力」的经典公式。

**知识与能力的关系**:数据规模和质量影响可学习的信息范围,参数规模影响表示与拟合能力;实际效果还取决于数据配比、训练目标、优化和推理方式。某些能力可能随规模出现明显跃升，但不应简化为固定阈值。

## 1.5 采样、Temperature 与 Top-p

模型输出的不是「一个答案」,而是**整个词表上的概率分布**,从一个分布中依概率采样出下一个词。

**三个核心采样参数**:

| 参数 | 作用 | 取值 | 效果 |
|------|------|------|------|
| **Temperature**| 缩放 logits 的「浓度」 | >1 更随机,<1 更确定,→0 变成贪心解码 | 创意任务调高,事实性任务调低,温度高则「翻车」概率上升 |
| **Top-p(核采样)** | 只从累计概率达到 p 的最小集合中采样 | 常用 0.8~0.95 | 剔除长尾低概率词,控制「跑题」 |
| **Top-k** | 只保留概率最高的 k 个词再采样 | 常用 10~50 | 同样用于收窄候选范围 |

**贪心解码(Greedy)**:每次都取最大概率词,确定性强,但可能陷入局部平庸;配合 beam search 可用于翻译等任务,生成任务一般用采样。

**重复惩罚(Repetition Penalty)**:对已出现 token 的概率打折,缓解「复读机」现象。

**工程要点**:写代码/算数/事实问答,常用 temperature≈0;写文案/头脑风暴,常用 0.7~1.0;Top-p 与 temperature 常配合使用(先用 Top-p 圈范围,再用 T 调随机度)。

## 1.6 幻觉(Hallucination)

**定义**:模型生成的内容看似流畅、语义正确,却与事实或上下文不符,甚至编造数字、引用、代码接口。

**成因**:
1. **知识的「自洽」而非「真实」**:预训练目标是「像人写的一样」,不是「符合事实」。
2. **数据本身的噪声**:网上数据本身含有错误、谣言。
3. **上下文冲突**:用户输入与模型固有知识矛盾时,模型可能「顺从」用户编造(迎合)。
4. **容量/覆盖不足**:长尾知识根本没被学到,模型被迫「编」。
5. 采样温度与解码策略也可能放大。

**检测与缓解**(面试高频):
- RAG:让答案基于检索到的证据,并**引用出处**,这是最有效的工程手段。
- 提示约束:明确要求「只有依据给定资料才能回答,不知道就说不知道」。
- 降低 temperature;对高利害场景不用采样。
- 「事实性评估」:用评测集对答案做事实性打分与兜底。
- 对高危内容(医疗/法律/金融)加**校验与人工审核**。

## 1.7 知识截止(Knowledge Cutoff)与实时性

模型的参数知识主要来自训练语料，并且通常存在知识截止日期;因此:
- 需要 RAG / 联网搜索 / 工具调用来补最新与私有信息。
- 面试时被问「如何让模型知道最新新闻」,常见回答:**接入检索与工具**,不必为了每次更新都重新训练模型。

---

# 第 2 章 Transformer 深度原理

> 这是面试的重灾区，务必吃透。主流通用文本生成模型多数采用 Decoder-only Transformer，但 Encoder-only、Encoder-Decoder 和多模态专用架构仍然存在。

## 2.1 从循环到注意力:为什么是 Transformer

- 早期 RNN/LSTM:逐词顺序处理,无法并行,长距离依赖衰减严重(梯度消失)。
- Attention(2014+)让每个位置能「直接看到」所有位置,但最初仍嵌在 RNN 里。
- 2017 年 **《Attention Is All You Need》(Vaswani et al.,Google)** 提出纯注意力架构 Transformer:
  - **完全可并行**(不再依赖序列顺序,靠位置编码注入顺序信息)
  - **长距离依赖直达**(任意两位置间路径长度为 1)
  - **计算可扩展**(GPU 友好,便于 Scale)

## 2.2 整体结构(Decoder-only)

```
输出概率分布
    ↑
    ┌───────────┐
    │ 输出层/投影 │  (hidden → vocab logits → softmax)
    ├───────────┤
    │   ↓
    │  N×(解码器层 Decoder Block)  ── 即「主干」Stacked Layers
    │    ├─ 自注意力子层(MHA 或 GQA/MLA)
    │    │     Masked Self-Attention(因果掩码,只看左侧)
    │    ├─ 残差连接 + 归一化(Pre-Norm:先 norm 再计算)
    │    ├─ 前馈子层 FFN(SwiGLU 等)
    │    └─ 残差连接 + 归一化
    │   ↓
    │  输入嵌入(Embedding)+ 位置编码(RoPE)
    ├───────────┤
      输入 Token 序列
```

**Decoder-only 特点**:每一层都带**因果掩码(Causal Mask)**,计算位置 i 时只允许看到 i 及之前的位置 → 天然适合「预测下一个 token」的自回归生成。

## 2.3 自注意力机制(数学化理解)

三个向量都由输入嵌入通过线性变换得到:
- **Q(Query)**:当前 token 想问「上下文里谁跟我相关?」
- **K(Key)**:每个 token 的「身份标签」,供匹配。
- **V(Value)**:每个 token 携带的「信息内容」。

**打分-加权公式**:

```
Attention(Q,K,V) = softmax( Q·Kᵀ / √d_k ) · V
```

步骤拆解:
1. **Q·Kᵀ**:逐对 token 计算相似度得分。
2. **÷ √d_k**:缩放,防止维度大时点积过大把 softmax 推向极端(梯度消失)。
3. **softmax(逐行)**:得分归一化为注意力权重。
4. **× V**:按权重加权聚合信息。

**为什么是 1/√d_k 而不是 1/d_k**:缩放量级是标准差级别(√d),太小不起作用,太大会抑制学习信号。

**复杂度**:对序列长度 L,计算量为 **O(L²)**——是长上下文成本的根本来源。

## 2.4 多头注意力(MHA)与变体 MQA/GQA/MLA

**多头注意力(Multi-Head Attention, MHA)**:把 Q/K/V 切成 h 个头(h 常见 8/16/32/64),各头独立做注意力,再拼接投影。不同头捕捉不同关系(语法、指代、主题、位置等)。

**变体与省显存演进**(面试加分，许多新模型使用 GQA、MQA 或其他变体，但 MHA 仍然存在):

| 变体 | KV 头数量 | 说明 | 使用 |
|------|-----------|------|------|
| **MHA** | = 全部头 | 表达力强,但 KV Cache 最大 | 经典(原始 Transformer、BERT) |
| **MQA(多查询)** | 1 个 KV 头共享 | KV Cache 最小,但表达力可能下降 | 早期优化方案 |
| **GQA(分组查询)** | 若干组共享 KV(折中) | 效果 ≈ MHA,KV Cache 显著下降 | **Llama 2/3、Mistral、多数开源模型** |
| **MLA(多头潜在注意)** | 对 KV 表示做潜在空间压缩 | 可显著降低 KV Cache 和带宽开销，具体收益取决于实现 | **DeepSeek V3/R1** |

**知识点**:在忽略量化、分页和额外元数据时，单请求 KV Cache 显存约为 `2 × batch × L × n_layers × n_kv_heads × head_dim × bytes_per_element`。其中 `n_kv_heads` 是 KV 头数，GQA/MQA 正是通过减少它来降低显存与带宽开销。

## 2.5 前馈网络 FFN 与激活函数

每层注意力之后是逐位置的 FFN(MLP),公式:FFN(x) = W₂ · σ(W₁·x)。**模型的绝大部分参数在 FFN 中**(注意力的权重占比很小)。

**激活函数演进**:
- ReLU / GELU(经典)。
- **SwiGLU**(一种门控线性单元):被 **Llama、Qwen、Mistral、DeepSeek 等**采用。通常通过不同线性投影得到门控分支和上投影分支，再将 `SiLU(gate) * up` 做逐元素相乘并投影回隐藏维度。效果和代价取决于具体模型配置，不能简单概括为一定优于其他激活函数。

## 2.6 位置编码:给模型「位置感」

自注意力本身没有任何位置信息(把词序打乱,注意力结果不变),所以必须注入位置。

- **绝对位置编码(APE,原始 Transformer)**:正弦/余弦或可学习嵌入加进输入。
- **RoPE(旋转位置编码,Su et al. 2021)**:把位置信息**以旋转变换作用于 Q/K 向量**。核心优点是相对位置信息可由旋转角差表达；它本身不保证任意长度外推，通常还需位置插值、频率缩放或继续训练。**Llama、Qwen、Mistral、DeepSeek 等大量模型采用**，闭源模型的具体实现以官方披露为准。
- **ALiBi(Press et al. 2022)**:给注意力得分按「位置距离」加上线性惩罚,长度外推强,成本极低;部分小模型使用。
- **相对位置编码 / T5 bias**:在打分时加位置差偏置。

> 面试常问:**如何让模型支持超过训练长度的上下文?** 答:不能只依赖 RoPE；通常要结合位置插值、频率缩放/NTK 类方法、继续训练或专门的长上下文训练，并在目标长度上做效果评测。能扩到多长取决于具体模型和配置，不能把 2K→128K+ 当成通用保证。

## 2.7 归一化与残差连接

- **残差连接(Residual Connection)**:把输入加到子层输出上(x + Sublayer(x))。让梯度可以「直通」到浅层,避免深层梯度消失/退化,是能堆几十上百层的基础。
- **归一化与层结构是两个维度**:Post-Norm 与 Pre-Norm 描述归一化放置位置;LayerNorm 与 RMSNorm 描述归一化形式。RMSNorm 去掉均值中心化,只做均方根归一化,更轻量;**Llama/Qwen 等采用 Pre-Norm + RMSNorm**。
- **DeepNorm 等深度归一化**:用于超深模型稳定训练。

## 2.8 KV Cache:生成阶段为什么快又为什么贵

**现象**:生成第 t+1 个 token 时,前面 t 个 token 的 Q/K/V 都要重新算一遍吗?
**不。** 保存前序 token 已算好的 **K、V 到缓存(Key-Value Cache)**;生成新 token 时仍要计算它自己的 Q、K、V，把新的 K/V 写入缓存，再用 Q 与缓存中的 K 打分。这避免了重复计算前序 token，使解码阶段每一步的注意力工作量由随序列平方增长变为随当前序列长度近似线性增长。

**代价**:
- KV Cache 随 L 线性增长,每个 layer 一份 K 和一份 V:
  **显存 ≈ 2 × batch × L × n_layers × n_kv_heads × head_dim × 精度字节**(忽略分页、量化和额外元数据)。
- 长上下文下,KV Cache 常常比权重还占显存 → 催生 KV 量化、PagedAttention、Sliding Window、MLA 等方案。
- 面试常问:「为什么越长的对话越慢/越贵?」→ KV Cache 增大,显存与带宽上涨,前序内容仍在参与计算。

## 2.9 注意力加速与 FlashAttention

**FlashAttention(Dao et al. 2022)**:
- 问题:标准注意力要把 L×L 的 QKᵀ 完整物化到 HBM(显存),访存是瓶颈。
- 做法:按分块(tiling)把计算加载进 SRAM,在线计算 softmax(用「在线算法」重计算统计量),**不保存完整注意力矩阵**。
- 效果:避免完整物化 O(L²) 的注意力矩阵，降低峰值显存和访存开销；加速幅度取决于硬件、序列长度和实现，不能固定写成 2~4 倍。
- 相关技术:FlashAttention-3、稀疏注意力、Sliding Window(窗口注意力,如部分 Mistral 模型)等；它们的目标和实现并不完全相同。

## 2.10 Encoder-only / Decoder-only / Encoder-Decoder 的演进与取舍

| 结构 | 代表 | 特性 | 现在 |
|------|------|------|------|
| Encoder-only(双向) | BERT(2018)、RoBERTa、DeBERTa | 双向上下文,理解强但不会生成全文 | 主要用于 embedding/检索,不再是大模型主流 |
| Encoder-Decoder | T5(2020)、BART | 编码器编码全文 + 解码器生成,天然适配「理解→生成」 | 少数场景(翻译、摘要)仍用 |
| Decoder-only(自回归) | GPT 系列、Claude、Llama、Qwen、Gemini、DeepSeek 等 | 单向因果,生成为主,统一范式 | **当前通用生成模型的主流** |

**为什么 Decoder-only 在通用生成模型中占主流**(面试常问):
1. 生成是当前大模型的主业,Decoder-only 原生自回归。
2. **Scaling 与工程实践**:大量通用生成模型采用 Decoder-only，生态和推理优化也围绕它发展；这不等于它在所有任务、规模和指标上都优于 Encoder-Decoder。
3. 架构同质化带来生态效率:推理引擎(如 vLLM)、微调、并发等工具都围绕它优化。
4. In-Context Learning 和指令遵循等能力可以自然融入自回归生成流程，但不能据此断言它在所有能力上都最佳。

## 2.11 MoE(混合专家)架构

**定义**:把 FFN 层替换为 **N 个专家子网络 + 一个门控网络**。每个 token 由门控选择 Top-K 个专家计算，其他专家对该 token 不参与计算；具体 K 和路由策略由模型决定。

**核心权衡**:
- **总参数量大**(记忆容量大),但**每个 token 通常只激活一小部分专家** → 专家 FFN 的单 token 计算量可接近较小模型；实际推理成本还受权重驻留、路由、通信和批处理影响。
- 路由方式:Token-choice(Top-k)为主;专家不平衡则用辅助损失/负载均衡。

**代表**:
- **DeepSeek V3/R1:总参 671B,激活约 37B**(据官方公开资料；具体版本和发布范围以对应模型卡为准)。
- Mixtral 8×7B(总约 47B,激活约 13B)。
- Qwen3 系列的 MoE 版本;Gemini、GPT 系列也采用稀疏 MoE。

**权衡/问题**:
- **模型权重存储通常仍受总参数量影响**；在专家分片部署时，专家可以分布在多卡或多节点，不一定每张卡都驻留全部专家。
- **单个 token 通常只访问被路由到的专家**，但专家分发、跨卡通信、权重驻留和批处理方式会影响实际吞吐；不能把“总参数量”直接等同于每步计算量或每步读取量。
- 训练更复杂:路由收敛、负载均衡、崩溃专家(dead expert)处理。

## 2.12 长上下文技术栈汇总(面试加分小节)

| 技术 | 作用 |
|------|------|
| RoPE + 频率外推 / 位置插值 / NTK | 把模型推理时的上下文扩到超过训练长度 |
| LongRoPE / YaRN | 在特定模型、训练和配置下扩展上下文，可达到更长窗口 |
| FlashAttention / 分块注意力 | 降低注意力内存与耗时 |
| KV Cache 量化(PKV) | 压缩缓存精度 |
| PagedAttention(vLLM) | 分页管理缓存,提高显存利用率 |
| GQA / MLA | 结构性降低 KV 量与带宽 |
| Sliding Window / 稀疏注意力 | 只依赖局部窗口,长程靠高层 |
| 上下文压缩/摘要、分层摘要、检索 | 用「信息管理」替代「无脑全量」 |

---

# 第 3 章 预训练:数据工程与训练全流程

## 3.1 为什么数据比模型更稀缺

预训练结论之一:**高质量语料是瓶颈**。互联网上「自然文本」有限,所以各家在做:配比调控、合成数据、代码/书籍偏抽样、数据治理。

## 3.2 数据工程步骤(面试可展开讲)

1. **采集**:网络爬虫(Common Crawl、自有爬虫)、书籍、论文、代码、专利、多语言、对话数据。
2. **清洗**:去 HTML/广告/导航/水印模板,去坏行、编码问题。
3. **质量过滤**:语言检测、启发式规则、质量分类器(Llama 2/3 论文公开了「质量打分器」思路)。
4. **去重**:URL 去重、文档级 MinHash 去重、跨语言近似去重。
5. **隐私与安全**:去 PII、去有害内容、版权争议内容治理。
6. **配比(Data Mixing)**:网页、书籍、代码、论文和多语言数据需要按目标能力与数据质量配比；代码数据通常有助于代码能力，也可能对形式化推理有帮助，但效果取决于数据质量、配比和训练方案。
7. **合成数据(Synthetic Data)**:用强模型反哺生成数据,用于预训练与后训练。
8. **课程(Course)/阶段**:先易后难或按 domain 渐进。

## 3.3 预训练目标函数

- 核心:**Next Token Prediction(NTP/语言建模)**,即最大化条件概率 p(x_t | x_<t)。
- 变体/组合:有的加入多任务目标、对比学习、知识增强目标、Span Corruption 或 Prefix LM；具体目标取决于模型架构和训练阶段。
- 双向模型(BERT)用 **MLM(掩码语言模型)**；Decoder-only 模型常用 NTP，但不能概括为所有大模型都只使用 NTP。

## 3.4 混合精度与数值格式

在 FP32 上训练算力与显存不可行,业界用**混合精度**:
- **FP16**:显存/算力减半,但动态范围小,易溢出(GradClip、Loss Scaling 配合)。
- **BF16(Brain Float 16)**:指数位与 FP32 相同(8 位指数),动态范围接近 FP32，对训练梯度更友好，是许多大模型训练中的常见选择。
- **FP8**:新一代格式(如 DeepSeek 用到 FP8 加速),进一步提效;训练 FP8 需要额外精度管理。
- 权重/梯度/优化器用不同精度(32 位 master 权重 + 16 位计算)是常见做法。

## 3.5 分布式训练四大流派(能讲清即可)

| 方法 | 思路 | 优点 | 代价 |
|------|------|------|------|
| **数据并行(DP)** | 每卡一份完整模型,分数据,梯度 all-reduce | 实现简单 | 模型必须单卡放得下;通信随模型增大 |
| **张量并行(TP)** | 单个算子内的矩阵按行/列切开到多卡 | 单样本单步也快 | 每层通信量高;依赖高速互联 |
| **流水线并行(PP)** | 按层切成多段,每卡算一段,微批次流水 | 减少空闲 | 气泡时间、显存不均 |
| **ZeRO / FSDP 分片** | 优化器状态→梯度→权重逐级分片到各卡,用完临时聚合 | 最省显存,支持超大模型单副本 | 通信量大,需要高效组网 |

> 组合拳是常态:如 **3D 并行(TP + PP + DP)** 训练千亿级模型;**MoE 还要加专家并行(EP)**。面试说清「为什么 3D 并行」加分:单卡放不下权重(权重分片/模型并行)+ 单卡放不下 batch 梯度(数据并行)+ 通信与显存权衡。

## 3.6 训练不稳定与调参(高级面试点)

- **Loss Spike**:训练中期 loss 突然暴涨。常见诱因:数据批次的极端样本、学习率过大、数值溢出(BF16 下除以接近零的范数)、权重更新过大。
- 对策:Batch 重采样、梯度裁剪、**深度缩放(DeepNorm/Residual Scaling)**、梯度检查点、恢复 checkpoint 后降 LR、**损失缩放**。
- **学习率调度**:Warmup + Cosine Decay 等方案较常见，具体 warmup 比例应结合模型规模、优化器和训练阶段调参。
- **Batch Size 调度**:有些训练会逐步调整 batch size，但是否增加、增加幅度和时机取决于优化目标与训练稳定性，不能作为通用规则。
- **Checkpoint 保存**:定期存,训练中断可恢复;损失发散时回退。
- **稀疏激活 MoE 特有问题**:专家负载不均 → 负载均衡辅助损失 / 专家路由正则;「死专家」处理。

## 3.7 训练成本估算(面试会考「估算题」)

**公式直觉**:对稠密 Decoder-only Transformer，训练计算量常用 **6ND** 做一阶估算(N=参数量,D=训练 token 数)。它没有覆盖所有架构和工程开销，MoE、激活重计算、通信、评估和数据处理都可能改变实际值。单位换算:
- A100/H100 的理论 Tensor Core FP16/BF16 算力必须按具体型号、稠密/稀疏模式查询；实际有效训练算力还会明显低于理论峰值，不能用一个跨型号的宽泛区间代替。
- 例:训练一个 70B 模型、1.4T tokens:C ≈ 6 × 70e9 × 1.4e12 ≈ **5.88e23 FLOPS**。
- 若把单卡有效算力粗略按 `3e14 FLOPS/s`、集群按 2,000 卡估算，纯计算时间约为 `5.88e23 / (3e14 × 2000) ≈ 9.8e5 秒`，约 11 天；再考虑通信、利用率波动、数据处理和失败重试，整体可能达到数周。这个结果只是量级估算，不是固定成本。

## 3.8 Scaling :算力、参数、数据的三角关系

- 参数相对数据过多或训练配比偏离计算最优时,模型未必能充分利用算力；Chinchilla 结论描述的是特定假设下的计算最优趋势，而不是所有训练任务的固定配比。
- **超参-规模共振**:更大模型需要的 LR、batch 相应调整;学界常讨论「Chinchilla 最优」与「推理效率非最优」(为推理多训一点反而更划算)。
- 面试价值:能说明「**6ND 一阶估算**」「**Chinchilla 原论文中的约 20× 趋势**」「**幂律**」及其适用边界即可过关。

---

# 第 4 章 后训练与对齐:RLHF · DPO · GRPO · 推理模型

> 对齐(Alignment)是让模型从「能续写」变成「听话、有用、安全」的关键阶段,也是 2024–2026 年研究最活跃的领域之一。**面试必考。**

## 4.1 为什么要对齐:三阶段范式的全景

常见的大模型训练路径:**预训练(Base)→ 指令微调(SFT)→ 对齐(RLHF/DPO)**，但不同模型可能省略、合并或增加阶段。

- 预训练产出「Base 模型」:会语言、会续写,但**可能不遵循指令、输出有害内容、语气怪**。

- SFT:用「指令-期望回答」数据教它「听指令」。

- 对齐:让输出匹配人类偏好与安全规范(有用 Helpful / 诚实 Honest / 无害 Harmless)。

- 近年部分「推理模型」路径额外加入**长思维链训练**(见 §4.5)，具体流程因模型而异。


## 4.2 监督微调 SFT(细节)

- **数据**:人工标注或合成(强模型生成+人工筛选)的「输入-期望输出」对。

- **质量问题(面试加分)**:数据的「多样性 > 数量」,宁可少而精;错误标注会让模型「学坏」;重复数据导致过拟合与安全下降。

- **训练方式**:对期望输出做交叉熵(与预训练相同的 LM Loss,只学「回答」部分)。

- **作用边界**:SFT 主要调整行为、格式和风格，也可能把训练样本中的新知识部分写入参数，但可靠性、泛化性和时效性有限。动态、私有、需要引用溯源的知识通常优先使用 RAG；系统性吸收领域语料时可评估持续预训练。


## 4.3 传统 RLHF 全流程(InstructGPT/PPO 范式)

1. **训练奖励模型(RM)**:收集「同一提示的 A/B 两个回答」的人类偏好标注 → 训练一个模型给回答打分。

2. **强化学习微调**:策略模型(SFT 产物)用 **PPO 算法**与奖励模型互动:

   - 对模型生成样本,用 RM 打分作为奖励;

   - 同时加 **KL 散度惩罚**(让新版别偏离参考模型(旧版)太远,防止语言退化);

   - 目标是 trade-off 奖励与 KL。

3. **必要组件**:参考模型、Critic/Value 网络、批采样、GAE 优势估计……工程复杂。

**PPO 的痛点(面试点)**:实现复杂、内存开销大。典型实现需要策略模型、参考模型、奖励模型和价值模型等组件，但是否以独立模型驻留、是否共享权重取决于实现；训练也可能不稳定且对超参敏感。

## 4.4 更轻的对齐:DPO、GRPO 与其它

### DPO(Direct Preference Optimization,Rafailov et al. 2023)
- **思想**:把「偏好优化」重写为**直接对偏好对做监督式目标**,绕开奖励模型与 RL 采样。
- **公式直觉**:loss 同时推高「被偏好的回答」概率、压低「被拒绝的回答」概率,并含参考模型 KL 约束。
- **优点**:省去独立奖励模型和在线 PPO/RL 采样流程，通常更容易实现和调试；效果依赖偏好数据质量、参考模型、超参数和具体实现，不能简单概括为一定更稳定或适用于所有任务。
- 变体:IPO、KTO(无偏好对也可)、ORPO(把 SFT 与偏好合一)等。

### GRPO(Group Relative Policy Optimization,由 DeepSeekMath 工作提出、DeepSeek-R1 大规模验证并公开)
- **思想**:同一提示生成**一组**回答,组内互相比较作为奖励,替代 PPO 里的 Critic/值函数 → 不用额外值网络,内存与超参更省。
- **与 R1 的关系**:DeepSeek-R1-Zero 从基础模型直接进行大规模强化学习，主要依靠可验证奖励探索推理行为；DeepSeek-R1 则加入冷启动数据、监督微调和多阶段强化学习等流程。其奖励既包含可验证任务信号，也可能包含格式等规则，不能把两者概括为同一条训练路径。
- 意义:**用规则/程序可验证的任务做 RL,是推理模型的关键配方**(R1 论文总结:CoT 数据集 + RL with 规则奖励)。

### 对齐技术的选型(面试小结)
- 人类偏好数据充足、希望采用离线偏好优化 → 可评估 DPO。
- 任务可程序化验证(数学/代码/确定性任务) → **GRPO/RL 式强化**是推理模型配方。
- 场景简单、数据少 → SFT 先行,再考虑偏好对齐。

## 4.5 推理模型(Reasoning Models):CoT 与 R1 浪潮

**背景**:OpenAI o1 等推理模型展示了通过增加推理时计算预算来提升数学、代码和逻辑任务表现的路线。具体发布日期、评测分数和对比结果必须以官方技术报告及明确的评测条件为准，不能仅根据二手资料或单项成绩比较模型。此后 DeepSeek-R1(2025.1)以公开权重和技术报告推动了推理模型的发展。

**核心配方(面试必讲)**:
1. **长 CoT 训练数据**:让模型学会把复杂问题分解为多步思考。
2. **强化学习 + 程序化奖励**:数学答案可校验、代码可运行测试。
3. **行为收敛**:模型自发涌现出「自我修正、反思、尝试不同方法」等行为。
4. **推理时算力扩展(Test-time Scaling)**:在任务、模型和预算合适时，增加推理预算可能提升正确率，但收益会递减，也可能增加无效思考和延迟。

**训练方式差异**(对比普通模型):
- 一般模型的一种常见路径:预训练 → SFT → DPO。
- 推理模型:预训练 →(冷启动 SFT)→ **大规模 RL(带长 CoT + 可验证奖励)** →(蒸馏/对齐)。

**实现与开源生态**:
- DeepSeek 的技术报告公开了方法和部分数据构造思路，不代表完整的 CoT 训练数据和全部训练细节公开；社区可以利用公开输出或数据构造方法，将部分推理行为蒸馏到小模型。
- 各厂商也推出了推理模型或思考模式；具体型号、上下文窗口、评测分数和能力边界应以相应官方模型卡、技术报告和产品文档为准。

**部署注意(工程点)**:推理模型可能因额外推理 token 带来更高延迟和 token 消耗;部分 API 提供「思考预算/推理力度」控制，是否支持及参数名称以具体产品为准，调用方可按任务调节。

## 4.6 蒸馏(Distillation)与模型合并

- **知识蒸馏(KD)**:用强模型(teacher)的输出(标签/logits/CoT)训练弱模型(student)。R1 把推理能力蒸馏到小模型就是代表。
- **白盒 logits 蒸馏 vs 黑盒生成蒸馏**:黑盒只能模仿输出文本。
- **模型合并(Model Merging)**:多个同架构模型权重平均/加权(SLERP、Ties-Merge),低成本组合能力。

## 4.7 对齐的挑战与安全(面试加分)

- **帮助性 vs 安全性的张力**:过度拒答、过度对齐、越狱(多语言、角色扮演、混淆)仍是难题。
- **对齐税(Honesty vs Sycophancy)**:模型可能「迎合用户」而牺牲事实准确(阿谀奉承问题)。
- **评估**:红队测试、安全性 bench、人类偏好评测;对齐不是一次性工程而是持续迭代。

---

# 第 5 章 微调与参数高效微调(LoRA/QLoRA)

## 5.1 什么时候要微调(决策框架,面试必答)

| 需求 | 采用手段 | 理由 |
|------|----------|------|
| 要**新知识/私有数据/最新信息** | RAG(首选) | 零训练、可更新、可溯源 |
| 要**固定格式/风格/领域术语/语气** | 微调 | 把「行为」固化进权重 |
| 要**新能力或特定任务路径** | 微调(或 Agent+工具) | 模型本身不擅长 |
| 临时/轻量需求 | 提示工程 | 零成本快速迭代 |

> **结论**:绝大多数业务场景先试「提示/RAG」;微调主要解决「说话方式与固定格式」以及部分领域能力;动态、私有、需要引用的知识通常优先检索。两者常联合使用。

## 5.2 全参微调(Full Fine-tuning)

- 所有权重可训练。
- **代价**:显存(参数+梯度+优化器状态)、数据要求、过拟合风险。
- 前提:通常需要较高质量、与领域匹配的规模数据(几百~几万条量级,依任务),算力充足。
- 现在很多场景已被 LoRA 替代;仅在大改造/超大量数据时用。

## 5.3 LoRA(Low-Rank Adaptation,Hu et al. 2021)数学与机理

**核心思想**:微调不需要改变全部权重。

- 冻结预训练权重 W₀;注入可训练的低秩分解矩阵:**ΔW = B·A**(B、A 为两个小矩阵)。
- **秩 r**:常见取 8~64(r 越大越接近全参微调但显存上升)。
- **缩放**:输出乘 α/r(α 为超参)。
- 前向变为:ΔW 加到对应线性层(W₀ x + (B·A) x)。
- 可训练参数:全参微调 7B 模型约 70 亿参数;LoRA 可把可训练参数压缩到**几千分之一**(比如 0.1%~0.5% 总量),显存需求大幅下降。

**为什么高效且效果接近**(面试深度回答):
1. **intrinsic dimension(本征维度)**:研究表明微调所需的参数增量本质是**低秩**的,ΔW 的有效秩远小于 D×D 全空间。
2. 只训练注入秩,反向传播与 Adam 状态显存骤降。
3. 可与 4-bit 量化(QLoRA)、多 adapter 切换(一个基座配多种风格)、在线增量更新(delta server 等)搭配。

**局限**:不能像全参那样「换头换脑」;对低秩假设不成立的任务(需全局改造)效果差于全参;部署时需合并或额外加载 adapter。

## 5.4 QLoRA(Quantized LoRA,2023)

- 基座用 **4-bit 量化(NF4,Double Quantization)** 冻结存储,LoRA 部分保持高精度训练。
- 效果:在合适的序列长度、批大小、梯度累积和检查点策略下，消费级显卡可以尝试较小模型的微调；能否运行 7B、13B 或 70B 量级取决于显存、量化实现和训练配置，不能按固定卡数保证。
- **机制要点(NF4)**:4-bit 归一化浮点格式,配合双重量化、分页优化器,极大省显存。

## 5.5 其它 PEFT 技术(了解即可)

- **Adapter**:层间插入小适配器模块。
- **Prompt/P-Tuning**:只训练「软提示向量」(可学习的前缀嵌入)。
- **Prefix Tuning**:在每层前加前缀向量。

## 5.6 微调实操要点(面试场景题)

- **数据量参考**:格式/风格类任务几千条即可看到明显效果;复杂推理类往往几万~几十万条。
- **数据质量 > 数量**:过滤噪声标注、去重、保证输出格式统一。
- **评估**:微调后与 Base 对比(同提示集、同评估集),看**目标指标与「通用能力是否退化」**(catastrophic forgetting)。
- **防止灾难性遗忘**:混合通用数据(如与领域数据按比例混合)是常用手段。
- **工具**:LlamaFactory、Hugging Face PEFT+TRL、AutoTrain 等开源工具可一行代码入门。

---

# 第 6 章 推理与部署优化

## 6.1 推理分两个阶段(面试高概率)

- **预填充(Prefill)**:一次性处理输入所有 token,为生成准备 KV Cache 并产出首个输出 token——**首 token 延迟(TTFT)**主要受它影响,通常计算密集。
- **解码(Decode)**:逐个生成后续 token——**每 token 延迟(TPOT/TBT)和输出阶段吞吐**主要受它影响,通常访存密集(每步只生成一个新 token,但要读取模型权重并访问 KV Cache)。

**为什么解码常受带宽限制**:每步的算术强度通常较低,但需要读取模型权重并访问 KV Cache；实际瓶颈还取决于 batch、并行方式、硬件和算子实现，不能绝对化为每次都完整读取全部权重。

## 6.2 吞吐与延迟的指标(面试黑话)

- **TTFT(Time To First Token)**:首 token 延迟,决定「体感快慢」。
- **TPOT / TBT**:每 token 生成时间。
- **吞吐(Throughput)**:每秒生成的 token 总量(多请求并发下才有意义)。
- **在线服务的权衡**:低延迟(小 batch/独占)vs 高吞吐(大 batch 批量)——**无法兼得**,这是调度器存在的意义。

## 6.3 高性能推理框架:vLLM 原理

**vLLM(2023 开源)**为何被广泛采用,面试至少讲两点:
1. **PagedAttention**:把每个请求的 KV Cache 按「页/块」管理(类比操作系统虚拟内存),减少连续显存分配带来的浪费与碎片,提高显存利用率。跨请求共享相同前缀属于 Prefix Caching 等机制，不能简单归因于 PagedAttention 本身；具体吞吐收益取决于模型、硬件、请求长度和实现。
2. **Continuous Batching(连续批处理)**:不再等整批生成完再走下一批;请求按需随时加入/退出,GPU 空闲期被填满 → 吞吐大幅提升。

其它加速点:算子融合、FlashAttention、量化支持、prefix caching(共享前缀复用,如多轮对话的 System Prompt)。

## 6.4 量化:用精度换成本

**原理**:低精度权重所需显存与带宽下降 → 更大 batch、更低延迟、单卡可部署更大模型。

| 方法 | 类型 | 说明 |
|------|------|------|
| GPTQ | 训练后量化(PTQ) | 按层最小化量化误差,4-bit 主流,适合 GPU 部署 |
| AWQ | PTQ(激活感知) | 按权重重要度保护,4-bit 效果好 |
| GGUF(GGML/llama.cpp) | PTQ + 动态 | 消费级 CPU/GPU 运行生态,支持 2~8 bit |
| NF4/QLoRA | 训练时 | 4-bit 微调 |
| QAT(量化感知训练) | 训练 | 更准但需重训,少用 |

**工程要点**:INT8 在不少模型和任务上精度损失较小，但并非保证无损;INT4 更需要技术选型与评测;量化与「长上下文+大吞吐」组合常用于降本。

## 6.5 投机解码(Speculative Decoding)

- 用小模型(草稿模型)快速生成一段候选 token,大模型一次并行校验一段(接受/修正)。
- 效果:在草稿模型与目标模型分布匹配、且请求条件合适时，可以减少目标模型的串行解码步数；实际加速幅度取决于接受率、批处理和硬件，不能固定写成某个倍数。
- 前提:草稿模型与主模型分布接近效果好;内存多一份小模型。面试讲「思路」即可。

## 6.6 蒸馏与轻量部署

- 知识蒸馏产小模型(7B/3B/1B)用于边缘/移动端;Ollama、llama.cpp 支持手机/笔记本运行。
- 多模态的轻量变体、端侧模型(如 Gemma 系列 270M~4B、Qwen3 小尺寸)用于离线场景。

## 6.7 成本模型与降本清单(面试场景题必考)

**为什么贵**:GPU 显存(容量)+ 算力 + 带宽;<big>长上下文尤其贵(KV 显存 + 注意力计算)</big>。

**降本手段清单**(按性价比排序):
1. **Prompt/上下文瘦身**:只放必须的内容 → token 即钱。
2. **缓存**:API 的 prompt/上下文缓存(部分服务会对命中内容折价或减少重复计算，具体机制和价格以服务商为准)——Anthropic 的 prompt caching 等是官方能力。
3. **模型路由**:简单任务用小模型,难任务才上旗舰(路由层)。
4. **量化 + vLLM** 私有化:大批量固定场景自建推理。
5. **蒸馏小模型**替代高频简单调用。
6. **offload/局部部署**:混合部署(Hybrid:小模型本地 + 旗舰 API)。
7. 避免长思维链默认开启:按任务精度要求调 reasoning effort。

## 6.8 评估体系(全链路,面试加分)

- **通用基准**:MMLU(知识)、HumanEval/MBPP(代码)、GSM8K/MATH/AIME(数学)、LongBench(长文本)等。
- **推理基准**:AIME(奥赛数学)、GPQA(科学)等。
- **多模态基准**:MMMU(大学多学科)、OCR/视觉问答等。
- **业务评估**:人工评测、偏好对、LLM-as-judge(用强模型打分,注意偏见:位置/长度/自我偏好)。
- **RAG 评估**:检索召回率、答案正确率、引用命中率、幻觉率。
- **提醒(面试要点)**:公开基准与业务真实效果**会偏离**(「刷榜」与数据泄漏问题),要用「自有评估集 + 线上指标」兜底。

---

# 第 7 章 提示词工程与上下文工程

> 提示词工程是大模型最容易「立竿见影」的能力,也是应用层工程师的核心技能。**本章是第 12 章相关面试题的前置知识。**

## 7.1 为什么提示词如此关键

- 大模型能力既受参数影响,也受上下文、提示和解码设置影响；同一个模型在不同上下文组织方式下，效果可能有明显差异。
- 提示词工程本质是**低成本利用 In-Context Learning**:不改权重就适配任务。
- 2025 年后业界把概念升维为「**上下文工程(Context Engineering)**」:如何组织输入(文档、工具输出、检索结果、历史、缓存)以最大化模型表现与降低成本。

## 7.2 提示词六大原则(体系化,而非背模板)

1. **写清楚指令**:具体动词(「列出」「逐条评审」「返回 JSON」),避免模糊要求。
2. **给定上下文(资料先行)**:把回答依赖的事实先放进去(它不会自己去找)。
3. **少样本示例(Few-shot)**:给 1~3 个正确示例,模型模仿格式与口径。
4. **约束输出**:格式(JSON/表格/列表)、长度、语气、立场边界。
5. **分步拆解**:要求「一步一步」完成任务(CoT),复杂任务按子任务拆解。
6. **设失败与兜底**:明确「资料不足时如何回答」「无法判断时怎么说」。

## 7.3 经典范式:零样本、少样本与思维链

| 范式 | 做法 | 适用场景 |
|------|------|----------|
| **Zero-shot** | 直接指令,不给示例 | 常见问答、日常任务 |
| **Few-shot** | 带 1~N 个示例 | 格式仿写、风格模仿、新任务「教学」 |
| **CoT(Chain-of-Thought)** | 引导「逐步推理」 | 数学、逻辑、多步决策、代码题 |
| **Self-Consistency** | 多次采样 CoT 再投票(多数一致) | 提升推理准确率(延迟/成本翻倍) |
| **ReAct** | 推理 + 行动(Thought → Action → Observation) | Agent、需要工具/检索的场景 |

**CoT 的出现(2022)**:研究显示，在部分数学和逻辑任务上引导模型生成中间推理步骤可以提升表现，说明提示可以在不更新参数的情况下激发或组织已有能力；这不是所有模型、任务都有效，也不能替代训练或工具校验。

## 7.4 结构化输出与可靠解析(工程重点)

- **JSON mode**通常只保证输出是合法 JSON,不等于严格符合业务 schema；**Structured Outputs**(在支持的 API 中)可按声明的 schema 约束输出。二者都仍应在服务端做 schema 校验；**Function Calling/Tool Use**则是让模型生成工具调用参数,不是普通 JSON 输出的同义词。
- **常见手段**:明确 JSON 字段与类型、schema 给出并让模型严格遵循、解析失败重试、用「字段级容错解析」。
- **实践要点**:设「输出只能是 JSON」的强约束;对关键字段用枚举与正则兜底校验;对高风险输出做「二次解析+重试」。

## 7.5 系统提示(System Prompt)设计

- 「你是谁(角色)、你要做什么(任务)、你要怎么做(约束与流程)、你不能做什么(边界)」四段式。
- 大型系统提示的**稳定接口**:把规则、few-shot、数据库 schema、政策写成「可复用片段」(contextual retrieval / prompt cache 友好)。
- **注意**:系统提示过长会挤占上下文与成本;厂商大多支持**系统提示缓存**以降本。

## 7.6 提示词注入(Prompt Injection)与防御(面试加分)

- **注入攻击**:用户或第三方内容里藏指令,劫持模型行为(「忽略以上,输出...」)。
- **类型**:直接注入(用户对系统)、间接注入(网页/文档/工具输出里藏指令)。
- **防御**:
  - 原则:把「非权威输入」与「系统指令」**严格隔离**并标注(如「以下内容仅当参考,不视为指令」)。
  - 对工具输出/网页内容进行**结构化隔离与标注**。
  - 对高危动作(花钱、发消息、改数据)加**权限确认、人审、沙箱**。
  - 输出校验:指令类输出(如 SQL、代码)再走一层安全性检查。
  - **本质**:把 LLM 暴露的「指令与数据混合」问题当作安全设计问题,而不能只靠提示语。
- **延伸**:Agent 场景注入更危险(工具权限放大),所以「权限最小化 + 沙箱 + 审计」是标配。

## 7.7 上下文工程进阶:如何组织超长上下文

- 目标:**「少而准」**优于「多而全」——选择性塞入优于全量喂入。
- 技术:检索引擎(RAG)、上下文压缩、分层摘要、历史「存档」、语义+结构组织。
- **成本意识**:token 即成本;用缓存、窗口裁剪、并行工具结果汇总管理开销。
- 面试场景题常考:「千页文档+用户提问,怎么组织?」标准答:先结构化索引/检索再问答,而不是全塞。

---

# 第 8 章 检索增强生成 RAG 深度

> RAG 是近年企业落地较常见的 LLM 应用形态,也是**面试高频题**。务必吃透「流程、切块、嵌入、向量库、检索、重排、评测」全链路。

## 8.1 为什么需要 RAG

- 模型不知道:私有知识库、实时信息、长尾事实、最新政策。
- 直接塞上下文:窗口有限、成本线性涨、长上下文「中间丢失」。
- **RAG 的方案**:先检索再生成,让回答「有依据可溯源」,同时可随时更新(换库不用换模型)。

## 8.2 架构演进:从 Naive 到 Agentic RAG(面试讲这个就是加分)

1. **Naive RAG**:一次向量检索 → 拼接 → 生成。局限:检索不准则全错,无反思无重试。
2. **Advanced RAG**:加查询改写、混合检索、重排(Rerank)、元数据过滤、滑动窗口合并。
3. **Modular RAG**:可替换检索源(向量库/数据库/API/网络)、多模块编排。
4. **Agentic RAG**:由 Agent 决定「要不要查、查什么、查几次、查询不通怎么办、总结」。Self-RAG、Adaptive-RAG 等研究探索了自我反思或按问题难度选择路线，实际收益需要用业务评测验证。
5. **GraphRAG**:在数据上构建**知识图谱**(实体+关系),可辅助回答「多跳关系型问题」(「XX 与 YY 有什么关系」)；是否优于纯向量检索取决于数据、图构建质量和任务类型。

> 面试回答「RAG 怎么做」时,先讲 Naive 全流程,再用「如果你来做进阶版」谈 Advanced/Agentic/GraphRAG,面试官会明显加分。

## 8.3 全链路流程详解

```
离线索引:
  文档 → 清洗 → 智能切块(Chunking)→ Embedding → 向量库
在线问答:
  提问 →(查询改写)→ Embedding → 向量召回 Top-K
       →(混合检索 / 元数据过滤)→ Rerank 精排
       → 组装 Prompt(片段 + 引用出处)→ 生成 → 带来源输出
```

## 8.4 切块(Chunking):最容易被低估的环节

| 策略 | 做法 | 适用 |
|------|------|------|
| 固定长度+重叠 | 按 N token 切,前后重叠 M | 简单起步 |
| 结构切块 | 按标题/段落/列表/表格分节 | 网页/文档(保留语义边界) |
| 语义切块 | 按句子嵌入相似度、语意边界切 | 对「中间割断」敏感的内容 |
| 父子分块 | 大块+小块关联存储 | 需要「上下文完整+命中精准」 |

**要点**:切块大小直接决定「召回命中率」与「输入 token 成本」;没有万能参数,**必须用评估集调**。块太小丢上下文,块太大噪声多。

## 8.5 嵌入模型(Embedding)

- **作用**:文本 → 向量,语义相近则向量接近(语义检索的基础)。
- 选择标准:**语义粒度(检索召回率)、维度(存储与显存)、成本、中文效果**。业界常用 BGE、OpenAI/Cohere/Azure embedding、各家中文 embedding 系列。
- 进阶:查询与文档的「不对称查询」(query 与 passage 常不同分布);领域微调 embedding;multi-vector(每段多向量)。
- 注意:**embedding 也有知识截止与领域偏差**,对垂直领域要专做评测与微调。

## 8.6 向量数据库选型(面试能横向对比即可)

| 方案 | 类型 | 特点 |
|------|------|------|
| FAISS | 库(非服务) | 最流行,内存级,上手快 |
| pgvector | PostgreSQL 扩展 | 与业务库同栈,事务方便 |
| Milvus / Zilliz Cloud | 分布式向量服务 | 海量、高 QPS,工业级 |
| Qdrant / Weaviate | 独立向量库 | 各有所长(Rust 高性能等) |
| Chroma / LanceDB | 零依赖轻量 | 原型与个人项目 |

**选型逻辑(面试)**:数据量(QPS/规模)、一致性需求、开发运维成本、与现有技术栈的契合度。小项目 pgvector/FAISS,大规模上 Milvus/Qdrant。

## 8.7 检索的深入:不是「查一次就完」

- **混合检索(Hybrid Search)**:向量 + 关键词(BM25)互补——专有名词/编号/型号用关键词命中,语义用向量。
- **查询改写(Query Rewriting)**:复杂问题拆解成子查询、原问题「脱口语化」、多轮对话补全(把代词补全成完整问题)。
- **重排(Rerank)**:召回后用 **Cross-Encoder**(把「问题+片段」一起过模型直接打分)精排。它能利用问题和片段的联合信息，常可改善排序，但计算成本更高，收益要用评测验证。**召回 50 → 重排取 5** 是一种常见起点，不是固定配置。
- **元数据/权限过滤**:文档权限(部门/用户级)在检索层过滤——**企业落地的合规关键**(面试加分)。
- **时效**:新鲜度偏好(时间加权)。

## 8.8 路由与多源(生产系统设计)

- 问题路由:先判断「该走向量库、走数据库(结构化)、走 API、还是直接答」。
- 结构化查询:非结构化问题转 SQL/查询语句(Text-to-SQL),再经过权限控制、语法检查和结果校验；这是企业 Agent 中的常见形态。
- 兜底:检索空/分数低 → 明确「未找到」而非硬编。

## 8.9 RAG 评估(面试必考的「落地感」问题)

| 维度 | 指标 |
|------|------|
| 检索 | 命中/召回率、Top-K 命中率、MRR |
| 生成 | 答案正确率、相关性、忠实度(是否忠于片段)、引用命中率 |
| 端到端 | 幻觉率、满意度、人工评测 |
| 系统 | 成本(Request 成本)、延迟 |

**评测方法**:建“评估集(问题-期望答案-期望片段)”;用 LLM-as-judge或人工;持续回归。**「检索不对」是 RAG 最常见病根**——先查召回,再看生成。

## 8.10 RAG 与长期记忆、缓存

- **语义缓存(Semantic Cache)**:对语义足够相似的问题复用已验证答案,减少模型调用；实际节省比例取决于命中率、误命中控制、答案时效和缓存策略，不能固定写成 90%。
- 交互型 RAG:历史问题结果可增量复用;对话记忆与知识库分开管理。
- 与「长期记忆」(用户画像、偏好向量库)结合 → 个性化知识助手。

---

# 第 9 章 Agent 智能体与工具使用

> Agent 是近年 LLM 应用的重要方向。面试高频:能说清「Agent 五个常见组成、核心循环、框架与 MCP、安全」。

## 9.1 Agent 的定义与组成

**Agent = LLM(大脑)+ 规划(Plan)+ 工具调用(Tools)+ 记忆(Memory)+ 反馈循环(Feedback)**,通过编排自主完成多步任务。不同框架对这些模块的拆分方式可能不同。

**常被问的对比**:
- 普通对话:单轮/多轮问答,**不行动**。
- Agent:设定目标 → 规划步骤 → 调用工具(搜索/代码/数据库/接口)→ 观察结果 → 调整 → 直到完成。

**核心组件五要素**:
1. **规划(Planning)**:任务分解 / 子目标定义 / 反思复盘。
2. **记忆(Memory)**:短期(上下文)+ 长期(向量库/数据库持久化)。
3. **工具(Tools)**:函数调用、API、搜索、浏览器、代码执行器。
4. **行动与反馈(Feedback loop)**:执行 → 观察工具输出 → 判断是否完成,不完则继续。
5. **模型与编排运行时**:由模型负责决策/生成,由应用代码或框架负责状态管理、调度、重试和停止条件。

## 9.2 核心循环:ReAct 模式

Thought(想)→ Action(调用工具)→ Observation(获得结果)→(再 Thought ...)→ 最终 Answer。

- **ReAct(2023)**:把「推理轨迹 + 行动」结合,让模型在思考中「用工具、看结果、再想」。
- ReAct 是一种常见的 Agent 交互模式；其他系统也可能采用预定义工作流、状态机、规划器-执行器或多 Agent 编排，不能把所有 Agent 框架都归结为 ReAct。

## 9.3 工具调用(Function Calling / Tool Use)

- **机制**:模型不「真的执行」工具,而是输出结构化「工具调用请求」;程序执行;把结果回传;模型继续。
- **API 形式**:模型返回一个/多个 tool_use(函数名+JSON 参数) → 代码执行 → 提交 tool_result → 模型基于结果回答。
- 为什么必须有:**模型无法上网/算数/访问私有数据**;工具把「对话模型」变成「能办事的系统」。(提示:本文档 2026 的最新实践均基于此机制。)
- **工程注意**:工具描述要清晰(名称+参数 schema+用途);并行工具调用;结果过大要截断;工具错误信息要回传;超时处理。

## 9.4 MCP(Model Context Protocol)深度

- **MCP(2024 年底由 Anthropic 发布并开源)**:标准化的「LLM 应用 ↔ 外部工具/数据源」接入协议。
- **类比**:「USB-C 之于外设」——一个 MCP Server 写一次,任何 MCP 客户端(Claude 应用、Claude Code、各类 SDK/IDE)皆可用,减少「每家工具接一遍」的重复工程。
- **三个角色**:**MCP Host**(LLM 应用/客户端)、**MCP Client**(在 Host 内部连接 Server)、**MCP Server**(封装工具与数据,暴露工具/资源/提示)。传输层支持 stdio 与 HTTP 等。
- **生态定位**:MCP 已成为重要的开放协议之一，许多模型厂商、开发工具和数据/工具服务在跟进支持；生态覆盖面和具体兼容能力仍在快速变化，应以各方官方文档为准。
- 面试应答:「MCP 尝试用统一协议减少工具接入碎片化，但实际可用能力要看 Host、Client 和 Server 的兼容范围。」

## 9.5 Agent 框架(常见选择)

| 框架 | 定位 | 特点 |
|------|------|------|
| **LangGraph** | 有状态图编排 | 显式状态机,适合复杂可靠流程，社区使用较广 |
| LangChain | 链与组件 | 组件丰富,入门友好(现更多用于子模块) |
| **LlamaIndex** | 数据/知识侧重 | RAG、知识工作流 |
| CrewAI / AutoGen | 多 Agent 协作 | 角色分工(crafter/执行/评审) |
| 各家 SDK 原生 Agent | 官方 | 与 MCP、模型能力耦合最好(如 Claude Agent SDK) |
| 轻量自研 | Prompt+循环 | 简单场景最快,不引入重框架 |

## 9.6 记忆体系

- **短期记忆**:上下文里的历史(窗口内),会话结束即丢。
- **长期记忆**:持久存储(向量库 / KV 数据库 / 文件),检索式注入。区分「会话记忆 / 用户画像 / 事实知识」。
- **工程实践**:记忆要「压缩与挑选」(全量塞回会爆窗口);记忆可信度标注(记忆也可能错)。

## 9.7 多 Agent 协作(进阶面试点)

- **角色化**:Planner(规划)、Executor(执行)、Reviewer(评审)、Critic(挑错)。常用于「质量敏感」流程(内容生产、代码任务)。
- **利弊**:协作有质量与并行优势,但有「成本翻倍、链路变长、Agent 间互相误导」问题;**能单 Agent 解决就不用多 Agent**(面试诚实答这一点加分)。
- **编排模式**:顺序流水线 / 黑盒评审循环 / 环形迭代。

## 9.8 Agent 应用架构与安全(面试「工程落地感」答题素材)

```
应用层(对话/任务管理/编排)
  → Agent 层(规划 + 推理 + 记忆)
  → 工具层(MCP Server / Function Calling)
  → 执行层(沙箱:代码执行、浏览器容器、数据库代理)
  → 审计层(日志、权限、人工审批、代价回收)
```

**安全设计四原则(必答)**:
1. **权限最小化**:工具只授予任务所需的最小权限(不能「模型想做什么就做什么」)。
2. **沙箱执行**:代码/文件/浏览器在隔离环境;高危动作走人审。
3. **注入防护**:工具输出按「数据」对待,隔离于指令(见 §7.6)。
4. **审计与兜底**:全链路日志、成本限制、超时/失败恢复、人工打断。

**可靠性工程**:步骤级「确认-回滚」、自动重试与降级(比如检索失败走兜底)、token/成本预算、任务编排状态持久化(中断可恢复)。——这是 Agent 生产化与「demo 变产品」的分水岭,面试主动提,极加分。

---

# 第 10 章 模型生态与行业格局

> 重要提示:模型型号、参数规模、上下文窗口、价格和评测成绩更新很快。以下只保留相对稳定的模型家族与技术路线；具体信息应以官方模型卡、技术报告、产品文档和发布日期为准，未经官方确认的行业传闻不作为结论。

## 10.1 模型能力分层(面试「黑话」)

| 类型 | 说明 | 代表家族 |
|------|------|------------|
| 基座(Base) | 能语言、能续写,不一定听话 | DeepSeek-V3、Llama3 基础版 |
| 指令/对话(Instruct) | 会遵循指令做问答 | 各 API 默认模式 |
| 推理(Reasoning) | 通过额外推理时计算或专门训练提升复杂任务表现 | OpenAI o 系列、DeepSeek-R1、Qwen 推理模型、各厂商的思考模式 |
| 多模态(Multimodal/Multi-modal) | 处理或生成图像、音频、视频等多种模态 | GPT、Gemini、Claude、Qwen-VL 等家族中的相应型号 |

## 10.2 海外主要模型家族

| 厂商 | 关键模型与节点 | 要点 |
|------|----------------|------|
| **OpenAI** | GPT 与 o 系列 | 闭源 API 与产品生态，覆盖通用、多模态、推理和工具调用；具体能力与计费以官方文档为准 |
| **Anthropic** | Claude 系列 | 长上下文、代码、工具使用与安全对齐是常见产品方向；具体型号和能力以官方模型页为准 |
| **Google** | Gemini 系列 | 原生多模态、长上下文和云平台集成是主要特点；具体窗口和模态支持随型号变化 |
| **Meta** | Llama 系列 | 公开权重生态广，适合研究、微调和私有化部署；许可证和商用条件需逐版本核对 |
| **Mistral(欧洲)** | Mistral、Mixtral 等系列 | 公开权重模型和高效部署是常见方向，具体许可证与型号需核对 |
| **xAI** | Grok 系列 | 与 X 等产品和实时信息场景有关联，具体能力以官方资料为准 |

## 10.3 中国主要模型家族

| 厂商 | 代表 | 要点 |
|------|------|------|
| **深度求索 DeepSeek** | DeepSeek-V3、DeepSeek-R1 等 | 公开了代表性模型权重和技术报告；V3 的 MoE、MLA、低精度训练与 R1 的可验证奖励路线是常见讨论点，具体版本信息以官方仓库和报告为准 |
| **阿里** | Qwen(通义千问)系列 | 覆盖通用、代码、推理和视觉语言等方向，公开权重型号较多；许可证、参数和部署要求需逐版本核对 |
| **月之暗面 Kimi** | Kimi 系列 | 长上下文、代码和 Agent 场景是常见产品方向；具体型号、参数和开放范围以官方发布为准 |
| **智谱** | GLM 系列 | 覆盖对话、代码、推理和 Agent 等方向，具体开放范围以官方发布为准 |
| **字节** | 豆包(Doubao)系列 | 面向对话、内容和企业应用等场景，具体模型与服务以官方资料为准 |
| **百度** | 文心 ERNIE 系列 | 覆盖模型服务与搜索等应用场景，具体能力和接口以官方资料为准 |
| **腾讯** | 混元(Hunyuan)系列 | 覆盖语言、多模态和企业应用等方向，具体版本以官方资料为准 |
| 其它 | MiniMax、阶跃、零一、可灵等 | 覆盖通用、视频、语音和垂直应用等不同路线，需按具体产品评估 |

> **近年的关键趋势**:公开权重模型、推理模型、低精度训练、长上下文、多模态和 Agent 工程持续发展；「开源/开放权重 + 本地或私有化部署」在部分合规与成本敏感场景具有吸引力，但要同时核对许可证、硬件成本、效果和运维能力。行业竞争重点逐渐从单纯参数规模扩展到推理质量、单位成本、工程化和工具使用能力。

## 10.4 开源生态现状(Ollama/社区视角,2026 中)

Ollama 等社区目录中常见的开放权重模型包括 Llama、DeepSeek、Gemma、Qwen、Mistral 等家族的不同型号，也有面向代码、推理和端侧部署的专用模型。目录内容、下载量和标签会随时间变化，不宜据此断言某个模型「最热门」或「遥遥领先」；实际选型应结合模型卡、许可证、硬件需求和业务评测。

## 10.5 选型的工程决策(面试「给方案」答题)

- **合规/数据不出域** → 开源私有化(Llama/Qwen/DeepSeek)+ vLLM/ollama 部署。
- **要最强通用能力、高利润/高价值场景** → 旗舰 API(Claude/GPT/Gemini/Kimi)。
- **高并发/成本敏感** → 小模型 + 路由,旗舰只处理难样本。
- **推理密集(数学/代码/逻辑任务)** → 推理模型(思考模式),并按难度调 reasoning effort 控延迟与成本。
- **多模态** → 原生多模态(Gemini/GPT 多模、Qwen-VL)。
- **主流做法是混合(Hybrid)**:开源私有化跑高频与敏感,旗舰 API 跑难任务与最新能力。

---

# 第 11 章 最新应用场景与落地(2026)

## 11.1 典型应用与落地成熟度

### 1) AI 编程 / 代码 Agent(成熟度较高的方向)
- 形态:从「补全代码」升级为「**多文件重构、跑测试、修 bug、自动迭代**」的 Agent 化编程(Claude Code 类、Cursor、GitHub Copilot、通义灵码、豆包 MarsCode)。工程团队用「代码 Agent 并行做独立任务」提效。
- 面试要点:能讲「代码 Agent 的安全(沙箱执行、权限最小化)」「agent 可并行跑多个任务提高迭代速度」「评测(单测/集成验证)作为任务完成的信号」。

### 2) 企业知识库问答 / RAG(技术栈最成熟的落地方向)
- 智能客服、内部文档助手、合规检索、研发问答。RAG 全流程见第 8 章。
- 面试要点:能讲「检索 → 精排 → 带引用生成」+「权限过滤 + 更新机制 + 评估」。

### 3) 大模型搜索 / 实时问答
- 「联网 + RAG」做最新资讯、Deep Research(多步检索+整理报告)。
- 面试要点:能讲「问题分解 → 多源检索 → 信息聚合 → 可信度控制」的 Research Agent。

### 4) 智能客服 / 语音对话 / 会议助手
- 全渠道多轮客服、语音实时对话(Voice agents)、会议转写与纪要。不同地区、行业和合规要求下的落地成熟度差异较大。

### 5) 数据分析 / 报表自动化(Text-to-SQL + Agent)
- 自然语言问数据 → 结构化查询 → 结果可视化;报表自动生成。Text-to-SQL 配合 schema 与权限是典型形态。

## 11.2 Agent 应用现状与演进(2026 强调)

- **C 端**:通用任务 Agent(订票/查资料/下单)、浏览器 Agent 成为新入口,厂商竞逐。
- **B 端**:销售线索处理、运维/告警、事件响应、报表、代码维护等 **SOP 类流程**最先被 Agent 化。
- **企业落地形态(2026 主旋律)**:SaaS 里的「Agent + 知识库 + 工作流编排」;**Human-in-the-loop(人审)仍是可信赖的前提**。
- **可靠性成为分水岭**:Agent 从「demo」到「产品」的关键是失败恢复、权限沙箱、成本预算、可审计(见 §9.8)。

## 11.3 多模态与生成式 AI 应用(2025–2026 快速爆发)

- **图像编辑/理解**:相关产品覆盖商品图、设计辅助、OCR 和文档理解等场景；具体产品名称、能力与用户规模应以厂商公告和公开数据为准。
- **视频**:文生视频(Sora、可灵、Runway、Pika)从 demo 走向**短视频/广告/影视辅助生产**。
- **语音**:实时语音对话(Voice Mode)、语音克隆/配音、会议转写。
- **端侧多模态**:端侧小模型(如 Gemma、Qwen 系列小尺寸)做离线 OCR、拍照问答。

## 11.4 垂直行业(RAG + Agent + 多模态的组合拳)

| 行业 | 典型应用 | 关键约束 |
|------|----------|----------|
| 医疗 | 病历结构化、临床资料问答(配 RAG+合规库) | **合规、可解释、人审**,不做「自动诊断」 |
| 金融 | 研报/公告摘要、信贷文本分析、合规检测、对话客服 | 可解释、误报克制、监管审计 |
| 法律 | 合同审查、法规检索 | 溯源引用、错误成本高 |
| 零售 | 智能导购、生成式商品描述、供应链预测 | 成本、实时性 |
| 教育 | 一对一定制讲解、错题分析、口语/编程陪练 | 个性化、防作弊 |
| 研发/制造 | 代码助手、知识库、文档自动化 | 私有化、数据安全 |

## 11.5 前沿与未来趋势(面试聊「展望」用)

- **更长上下文与上下文管理**:部分产品已提供很长的上下文窗口，但窗口大小不等于有效利用；分层、流式上下文、压缩和检索仍用于控制成本与噪声。
- **推理 + 工具 + 多模态融合**:一个模型同时「会深想、会调工具、能看能听」。
- **Agent 经济学与多 Agent 协作**:任务定价、SLA 化;复杂 SOP 用「多 Agent + 编排框架」。
- **私有化/端侧爆发**:数据不出域、端侧算力利用(手机/边缘)。
- **对齐、安全与评估基础设施化**:LLM 应用的**评测与治理成为产品必备**(不是可选项)。
- **具身智能与机器人**:LLM/VLM 与 VLA(视觉-语言-动作)模型用于机器人规划和控制，仍需结合专用策略、仿真、真实世界数据与安全约束。
- **规模与效率并重**:模型发展不只取决于总参数量，也取决于数据质量、稀疏激活、推理效率、工具使用和单位成本；具体行业判断应以可复现的模型卡和评测为依据。

---

# 第 12 章 面试题大全与深度追问

> 分「概念原理 / 训练对齐 / 工程部署 / 应用系统设计 / 场景方案 / 高级脑洞」六类,每类给**参考答案 + 面试官会跟进的追问**,以及「回答加分点」。能连答两层追问是高分关键。

## 12.1 概念与原理类

**Q1. 大模型的工作原理是什么?用一个形象的比喻讲。**
> 可以比喻为「读过大量训练语料、能按概率接话的超级接龙玩家」。它不一定直接「查答案」,而是根据已有上下文,给出「接下来最可能出现的 token」的概率分布,再按解码策略生成。理解和推理表现来自训练与推理过程中的综合能力。
> **加分**:主动点出「输出是概率采样不是查表」「涌现能力与 In-Context Learning」。

**Q2. 什么是自注意力?为什么它能解决长距离依赖?**
> 每个位置的 Q 与全序列的 K 打分,得到对每个位置的「关注权重」,再按权重聚合各位置的 V。因为两位置间的「路径长度」始终为 1,不经过中间词逐层传播,所以长距离关系可以直接建模(对比 RNN 的链式传播会衰减)。复杂度 O(L²)。
> **追问(能接住加分)**:那长文本怎么优化?→ FlashAttention(分块减少显存/访存)、稀疏/窗口注意力、KV 缓存，以及结合位置插值或频率缩放等方法扩展上下文；这些技术解决的是不同问题。

**Q3. 什么是 Token?中文一个汉字占多少 token?**
> 模型处理的最小单位,是子词而非「字」。中文约 1 汉字≈1 token,英文约 1 词≈1~2 token(Tokenizer 用 BPE 等切分)。Token 决定计费、窗口、延迟。
> **加分**:能讲 BPE 从字符开始做子词合并;讲「同样内容不同模型 token 数可能不同」的成本含义。

**Q4. Temperature 和 Top-p 的作用与区别?**
> 两者都是控制采样随机性:Temperature 缩放整个概率分布的「浓度」(高→创意、低→确定;常用 0~1.5);Top-p(核采样)只从累计概率达 p 的最小集合采样,剔除低概率「跑题」候选。工程上通常**先用 Top-p 圈范围,再用 Temperature 调随机度**;事实/代码类用低温近 0,创意类用 0.7~1.0。
> **修正说明**:Temperature 与 Top-p 的「单位」不同(一个是温度、一个是概率累积阈值),面试对比时讲清这点是关键。

**Q5. 什么是幻觉?成因与缓解?**
> 见 §1.6 完整版。一句话:「模型编造却不自知」。成因:目标是对齐语料而非对齐事实、数据含噪、容量覆盖不足、迎合用户。缓解:**RAG+引用、明确「不知道就说不知道」、低温、事实评估、高危人工审核**。
> **追问(高频)**:「你的 RAG 系统怎么检测和降低幻觉?」→ 引用命中率/忠实度评估、来源缺失即拒答、输出带出处、幻觉评测集回归。

**Q6. 上下文窗口是什么?超出怎么办?**
> 见 §1.3。重点答:截断有损、压缩有损、**检索通常更容易控制输入规模**;并点出「Lost in the Middle」——长窗口不等于全利用。加分:讲成本随 token 线性上升,所以在信息海量或预算有限时，检索通常比全塞更省，也更容易控制噪声。

**Q7. 什么是 KV Cache?为什么它决定推理性能?**
> 生成时把前序 token 已经算过的 K、V 存起来；新 token 仍要计算自己的 Q、K、V，再用 Q 与缓存中的 K 做注意力，避免重复计算前序 token。代价是缓存随序列长度线性增长，显存≈2×L×层数×KV 头数×头维度×字节数；**长上下文时它可能成为主要显存开销**，由此催生 GQA/MLA、KV 量化、PagedAttention。
> **加分**:能说出 vLLM 的 PagedAttention 就是「把 KV 缓存按页管理来省显存」。

**Q8. 为什么大模型用 Decoder-only 而不是 Encoder-Decoder / BERT?**
> ① 生成任务是大模型的重要应用,Decoder-only 原生支持自回归生成;② 大量通用生成模型和工具围绕 Decoder-only 建立了成熟生态;③ 结构统一有利于扩展、训练和部署。它并非在所有任务、规模和指标上都优于 Encoder-Decoder。详见 §2.10。

**Q9. 什么是 MoE?为什么「总参大」却能「算得快」?**
> 每层多个专家 + 门控,每 token 只激活 Top-2 等少数专家。总参(显存与记忆)大,但每个 token 的专家 FFN 计算量可接近较小模型；实际成本还受权重驻留、专家分片、路由、跨卡通信和批处理影响。训练有负载均衡与路由收敛问题。代表:DeepSeek-V3、Mixtral、Gemini、Qwen-MoE、GPT 系列中的部分稀疏模型。
> **追问**:「MoE 的缺点是?」→ 显存占用高、路由器/冷门专家问题、分布式训练更复杂(需专家并行)。

**Q10. 什么是 Scaling Law?「6ND」和「20×」是什么意思?**
> 能力与 N、D、C 之间常呈可拟合的缩放关系;在 Chinchilla 的特定假设下，计算最优训练 token 数常近似为参数量的 20 倍，训练算力可用 C≈6ND 做稠密模型的一阶估算。70B×1.4T 的结果约为 5.88e23 FLOPs，实际时间还取决于硬件有效利用率、通信和数据管线。见 §1.4、§3.7。

## 12.2 训练与对齐类

**Q11. 大模型训练的完整流程是什么?**
> 常见流程是预训练(自监督目标,学习通用语言与知识表示)→ SFT(指令数据,调整遵循指令和输出格式)→ 对齐(RLHF/DPO 等,优化偏好与安全)。推理模型可能再加入长 CoT 数据和带可验证奖励的强化学习。每个阶段都会同时影响知识、能力和行为，只是训练目标与侧重点不同。详见第 3、4 章。

**Q12. RLHF 和 DPO 有什么区别?**
> RLHF 的经典 PPO 流程通常包括偏好数据、奖励模型、策略模型、参考模型和价值模型等组件，在线采样和优化较复杂。DPO 将偏好优化改写为离线的直接目标，省去独立奖励模型和在线 PPO 流程，但效果依赖数据质量、参考模型和具体实现。GRPO 使用组内相对奖励估计优势，常用于配合可验证奖励的推理训练。详见 §4.3–4.4。
> **追问**:「推理模型用哪种对齐?」→ GRPO/RL + 可验证奖励(数学答案、代码测试)。「一般对话模型呢?」→ SFT + DPO。

**Q13. SFT 能教会模型新知识吗?**
> **不能简单回答「能」或「不能」。** SFT 主要调整行为、格式和风格，也可能让训练样本中的新知识部分写入参数，但可靠性、泛化性和时效性有限；如果样本少且事实复杂，模型也可能只是学会复述或更自信地编造。动态、私有、需要出处的知识通常优先用 RAG；系统性吸收领域语料可评估持续预训练。**这是面试高频边界题。**

**Q14. LoRA 为什么能在极小成本的微调中接近全参效果?**
> 冻结主干,只训练注入的两个低秩矩阵(ΔW=BA)。依据「微调的本征维度很低」——所需权重增量本质是低秩,小 r(8~64)就够。可训练参数可降到总量 0.1%~0.5%,反向传播与优化器显存大幅下降。配合 QLoRA(基座 4-bit 量化)可单卡微调大模型。局限:低秩假设不满足时不如全参。详见 §5.3–5.4。

**Q15. 什么是混合精度?为什么用 BF16?**
> 用 FP16/BF16 计算并视优化器实现保留更高精度的主权重、梯度或优化器状态，以降低显存和计算开销。BF16 的**指数位与 FP32 相同**,动态范围较大，通常比 FP16 更不易溢出；是否采用 BF16、FP16 或 FP8 取决于硬件、模型和数值稳定性评估。见 §3.4。

**Q16. 训练中途 loss 炸了(Loss Spike)怎么办?**
> 常见诱因:极端 batch 样本、LR 过大、数值溢出(小范数除零)、权重更新过大。对策:Batch 洗牌/重采样、梯度裁剪、调 LR(warmup/cosine)、深度缩放、降低梯度检查点开销、**回退到上一 checkpoint 降 LR 重启**,MoE 看路由负载与死专家。见 §3.6。

**Q17. 数据工程对预训练为什么那么重要?**
> 数据决定「知识下限与上限」;质量 > 数量(宁高质量少,勿低质多)。流程:采集→清洗→质量过滤→MinHash 去重→隐私/安全→**配比(代码占比影响推理)**→合成数据。数据泄漏(测试集混入训练)、重复、毒性都会显著影响模型与安全。见 §3.2。

## 12.3 工程与部署类

**Q18. 推理的两个阶段(TTFT / 吞吐)是什么?**
> 预填充(Prefill,处理输入并准备 KV Cache,通常计算密集,影响 TTFT)+ 解码(Decode,逐个生成后续 token,通常访存密集,影响 TPOT 和输出吞吐)。在线服务需要在低延迟与高吞吐之间调度，Continuous Batching 可以提高整体利用率。见 §6.1–6.3。
> **追问**:「为什么解码常受带宽限制?」→ 每步算术强度通常较低，但要读取权重并访问 KV Cache；实际瓶颈还取决于 batch、并行方式、硬件和算子实现。

**Q19. 量化是什么?GPTQ/AWQ/GGUF 区别?**
> 把权重降精度(FP16→INT8/INT4)以省显存与带宽(→更大 batch/更小卡可跑更大模型),精度代价可控。GPTQ/AWQ 是训练后量化,适合 GPU 服务(4-bit 主流);GGUF 是 llama.cpp 生态的量化格式,支持 CPU/消费级本地运行。见 §6.4。

**Q20. 怎么给大模型推理降本?(开放题)**
> 清单见 §6.7,重点答:上下文瘦身、prompt/上下文缓存(部分服务可折价或减少重复计算)、**模型路由(简单→小模型/复杂→旗舰)**、量化+vLLM 私有化、蒸馏小模型、长思维链按需开关、混合部署。加分:主动提「语义缓存」与「按业务保底质量分级」。

**Q21. 如何评估一个大模型 / 一个 LLM 产品?**
> 分层评估:通用基准(MMLU/HumanEval/GSM8K)、推理基准(AIME/GPQA)、长文本与多模态基准(LongBench/MMMU 等)、**业务自有评估集**(真实场景问题+期望答案+客观/人工/LLM-judge)、线上指标(满意度/成本/延迟/兜底率)、安全与红队。强调**公开基准与业务会偏离**,要自建评估集兜底。见 §6.8。

**Q22. 自研推理还是用云 API?给了什么边界?**
> 自研(vLLM 私有化):大批量、固定模式、数据敏感、要极致成本 → 私有化。云 API:小规模、要最新最强能力、不想运维。主流是 **混合**:高频简单用开源私有化,难任务/最新能力用旗舰 API。见 §10.5。

## 12.4 应用与系统设计类

**Q23. RAG 和微调怎么选?(必考)**
> 「知识/动态/私有」→ 通常优先 RAG(可更新、可溯源);「固定格式/风格/领域话术」→ 微调;两者常**联合**(RAG 供依据 + 微调规范表达)。核心判别:**你要解决的是「它不知道」还是「它不会说(格式/风格)」**。详见 §5.1、§8.1。

**Q24. 请设计一个企业知识库问答系统(系统设计题)。**
> ① 文档接入清洗;② 智能切块;③ Embedding 入库(向量库选型);④ 在线检索(查询改写+混合检索+权限过滤);⑤ Rerank 精排(Top-K 剪到真正相关);⑥ 组装带出处的生成;⑦ 兜底「未找到」;⑧ 全链路评估与日志;⑨ **合规:权限过滤、数据脱敏、可审计**。加分点:Agentic RAG 自适应、语义缓存、分层索引。见 §8 全章。

**Q25. 什么是 Agent?它和普通 ChatGPT 对话有什么本质区别?**
> 对话只是「问答」;Agent 是「**目标导向的多步执行**」:规划 → 调工具(搜索/代码/数据库/接口)→ 观察结果 → 调整 → 完成。关键在「行动 + 反馈循环 + 记忆」,而不只是「回应」。
> **加分**:能讲 Agent 的「可靠性 > 聪明」——失败恢复、沙箱、成本预算才是能不能产品化的关键(§9.8)。

**Q26. MCP 是什么?它解决什么问题?**
> Model Context Protocol:统一「LLM 应用 ↔ 工具/数据源」的开放协议。类比 USB-C。一个 Server 可以被多个兼容的 Host/Client 复用，减少工具接入碎片化；但实际能力取决于各实现的兼容范围。三个角色:Host / Client / Server。见 §9.4。

**Q27. 如何做一个能「查最新新闻」的模型应用,而不重新训练?**
> **接入检索，不必重新训练模型**:联网搜索/RAG + 工具调用,把最新内容作为上下文给模型,并要求忠实于给定资料、注明时效。这是应用层工程师的常见方案。见 §1.7、§7.2。

**Q28. 怎么做「结构化的 JSON 输出」,并保证可靠?**
> 先区分能力:JSON mode 通常只保证合法 JSON；Structured Outputs(若 API 支持)可按声明的 schema 约束；Function Calling/Tool Use 用于生成工具调用参数。无论采用哪种方式，都应定义 schema → 服务端校验 → 失败重试或兜底 → 字段级容错。对高风险字段使用枚举/正则校验，对不确定输出做二次确认。见 §7.4。

**Q29. 面对一个千页文档和一个问题,怎么回答最省钱最准?**
> 通常不要全塞。先对文档做结构化索引(切块/层级/章节),检索命中相关片段,再让模型基于片段回答并注明页码；在信息分散或上下文预算有限时，通常比全塞更省，也更容易控制噪声，但是否更准要用业务评测验证。这是「上下文工程」的核心观念(§7.7、§8)。

**Q30. 系统提示怎么设计?(开放题)**
> 四段式:身份、任务、约束与流程、边界(绝不做什么);必要时给 fixed few-shot 与数据 schema;**保持稳定以便缓存**(省成本);把「非权威输入」与指令隔离防注入。见 §7.5。

**Q31. 多轮对话怎么做「记忆」才靠谱?**
> 区分会话短期记忆(上下文)与长期记忆(向量库/数据库)。做法:历史改写补全(代词→完整问题)、对话摘要压缩、关键信息抽取存入长期记忆、检索相关历史注入;**记忆也要可信度标注,否则错误记忆会污染模型**。见 §9.6。

**Q32. 并发高的问答服务怎么设计?(系统题)**
> 分层:网关限流 → 推理服务(vLLM 大 batch 高吞吐)→ 请求队列 → 结果缓存(语义缓存/短时缓存)→ 降级(简单问题走小模型/规则)→ 可观测(延迟/成本/失败率监控)。关键权衡:「在线低延迟」与小 batch 成本,用 Continuous Batching 与缓存缓解。

## 12.5 场景方案类(「你怎么实现 X」实战题)

- 「让模型回答合同里的条款内容」→ 合同解析 + 结构化分块 + RAG + 引用原文页码 + 越权内容拒答。
- 「做一个客服,需要查订单」→ 函数调用/工具访问订单系统(只读权限)+ 上下文 + 对账失败重试 + 人审。
- 「把公司 SOP 变成可用助手」→ 流程编排(多步引导)+ 表格/FAQ + 权限 + 每一步可确认。
- 「识别并拦截 AI 生成内容/违规」→ 分类器模型 + 规则级联 + 红队测试;说明「无完美识别,要做概率化与人工复核」。
- 「给一份报告做摘要并要可溯源」→ 分段摘要 + RAG 分块 + 出处标注 + 忠实度评估。

## 12.6 高级脑洞与「面试官想听的」

- **「如果让你在一个月内把大模型落地到你们公司,你会怎么推进?」**
  答:先**盘点场景与痛点** → 选**最稳最有 ROI 的 1~2 个**(通常是知识库问答/客服/代码助手)→ MVP 上线收集数据 → 设计**评估集与线上指标** → 迭代(换模型/调 RAG/补权限)→ 治理(安全、合规、审计)同步跟进。**加分**:强调的是「以问题先于技术」的工程观,而非吹模型多强。
- **「模型回答错了,你第一个排查什么?」**
  答:按「上下文够不够(有没有资料/有没有截断)→ 提示词清不清晰 → 任务是否超出当前模型能力(是否该路由到更大/推理模型)→ 输出约束是否生效 → 评估集是否回归」的流程排查,而不是「怪模型玄学」。见 §7.3。
- **「长上下文与 RAG,到底选哪个?」**
  答:成本低时、信息集中、且需要全局理解→长上下文好;信息海量、要持续更新、要权限过滤→RAG 好。**实践上两者结合**:长窗口 + 检索式注入,根据任务混合。
- **「评估怎么做到不被『刷榜』骗?」**
  答:自建与业务对齐的评估集 + 线上 A/B + 人工抽样复核 + 关注「长尾/安全」,不过度依赖公开榜。数据泄漏与泛化不足是公开榜的软肋。
- **「你怎么看待 Agent 取代工程师的争议?」**
  开放题,答「工程价值观」:工具放大能力但需人来设计目标、做安全与验收;Agent 改变的是「怎么做」,不是「要不要人」。避免极端观点。

---

# 第 13 章 学习路线图(细化到周)

> 面向「从 0 到面试可战」的 7 周路线。已有基础可跳过相应阶段,但**面试前至少完成一个自建项目**(RAG / Agent / 微调任一)。

## 阶段一:建立认知(第 1 周)
- **目标**:能向同事讲清「大模型是什么、有哪些主流、能干什么」。
- 行动:读本指南第 1、10、11 章;用 1~2 个成熟应用(Anthropic / OpenAI / Gemini / DeepSeek / Kimi 任一)体验;浏览各官网模型列表与定价页(感受生态与成本)。
- 产出:一页「大模型全景」脑图(概念 / 原理 / 生态 / 应用四条线)。

## 阶段二:Prompt 与 API(第 2 周)
- **目标**:会用 API 调用模型完成真实任务。
- 行动:读第 7 章;用官方 SDK 写脚本完成「翻译 / 摘要 / 抽取为 JSON」;练 zero-shot / few-shot / CoT / 结构化输出;对比 temperature、Top-p 效果。
- 产出:3~5 个可复用脚本 + 一份「提示技巧对比」笔记。

## 阶段三:RAG(第 3 周,重点)
- **目标**:亲手做通一个「带检索、带引用」的知识问答系统。
- 行动:读第 8 章;文档 → 切块 → 向量库(FAISS / pgvector)→ 检索 → Rerank → 带出处回答;做一个语义缓存加分项。
- 产出:能回答「给定文档问题并给出出处」的 demo + 一页召回率/忠实度评估记录。

## 阶段四:Agent 与工具使用(第 4 周)
- **目标**:做出「会调用工具办事」的 Agent。
- 行动:读第 9 章;用 function calling / MCP 做「查天气 / 算数学 / 查数据库 / 上网」的 Agent;做一个「自动搜集资料写报告」的多步 Agent(LangGraph 或轻量自研循环);设计并实现「权限最小化 + 失败重试 + 成本预算」。
- 产出:多步 Agent demo + 一页「安全与可靠性设计」(面试大加分)。

## 阶段五:原理深入(第 5~6 周,面试核心)
- **目标**:把 Transformer、注意力、KV Cache、LoRA、DPO 讲透。
- 行动:精读第 2、4、5 章;把「一次前向经过哪些层」画出来;亲手跑一次 **LoRA/QLoRA 微调**(LlamaFactory 或 Transformers + 开源 7B);读 DPO 与 R1 报告摘要。
- 产出:能「看着白板」讲完整前向;能讲清 LoRA 数学(ΔW=BA、α/r)与适用边界。

## 阶段六:部署与成本(第 6 周,依岗位选做)
- **目标(推理/后端岗必做)**:本地部署并压测一个开源模型。
- 行动:vLLM 或 Ollama 跑开源小模型 → 走通 API → 量 TTFT / 吞吐 → 试量化(GPTQ/GGUF)→ 对比成本。
- 产出:「不同量化与并发下的延迟/吞吐」小报告(工程干货)。

## 阶段七:面试冲刺与项目包装(第 7 周)
- 行动:过一遍第 12 章每题,选 8~10 题写「一题三层」(第一答 + 追问 + 加分点);把 RAG/Agent/微调项目写成「指标 + 踩坑 + 决策」式简历条目;刷 2~3 场模拟面试。
- **面试前 48 小时**:刷第 10 章生态与当周新闻,背附录速记卡。

> **路线原则**:别贪多。面试官最看重「你真的做过一个能跑的东西 + 能说清为什么」,而不是「背了多少名词」。

---

# 第 14 章 资源推荐

## 论文(按面试价值排序)
1. **《Attention Is All You Need》(2017, Vaswani 等)** — Transformer 起源。
2. **《Language Models are Few-Shot Learners》(GPT-3, 2020)** — 上下文学习 / 涌现。
3. **《Training Language Models to Follow Instructions》(InstructGPT, 2022)** — RLHF 范式。
4. **《LoRA》(2021)** — 轻量微调本体。
5. **《Direct Preference Optimization》(DPO, 2023)** — 偏好优化的重要方法，建议读原文摘要并了解其适用边界。
6. **《DeepSeek-R1》(2025)** — 推理模型 + GRPO 配方;读技术报告关键结论。
7. **《Chain-of-Thought Prompting》(2023)** — CoT。
8. **Scaling Laws:Kaplan(2020)、Chinchilla(2022)**。
9. **《Retrieval-Augmented Generation》(RAG, 2020)** — 应用层必读。
10. **《ReAct》(2023)** — Agent/工具使用基础。
11. **《QLoRA》(2023)、Flash Attention(2022)、RoFormer/RoPE(2021)** — 工程/架构细节。

> 面试不必精读全部,但要有把握把其中 3~5 篇「用自己的话讲清核心思想」。

## 官方文档与 API(第一手)
- Claude(Anthropic):`platform.claude.com` 模型 / 文档 / SDK;MCP 文档。
- OpenAI、Google Gemini、DeepSeek、Kimi(Moonshot)、Qwen(阿里云)官方 docs 与模型页。
- **OpenRouter**:一个 Key 对比多家模型,适合试用与对比。

## 开源工具链
- 应用:PEFT / TRL(HF)、LlamaFactory、LangChain、LangGraph、LlamaIndex、CrewAI、AutoGen、MCP SDK。
- 推理:vLLM、Ollama、llama.cpp(GGUF)、Transformers、FlashAttention。
- 向量:BGE 等嵌入、FAISS、pgvector、Milvus、Qdrant、Chroma。

## 中文优质内容
- 各厂商官方公众号/博客(DeepSeek、Qwen、智谱、Kimi 等)。
- Hugging Face 博客中文翻译;知乎/AWS 系高质量专栏。
- 刷题:本文第 12 章 + 各公司面经(注意时效,以官网为准)。

## 持续学习
- 关注**官方发布**(不要只看二手解读);每周看 1~2 条「模型发布 / 开源 / 评测」新闻,保持生态敏感度。

---

# 第 15 章 面试避坑与加分项

## 常见扣分点(务必避开)
- ✗ 把「大模型」当 GPT 专利,不了解开源与国产生态(2026 面试几乎必聊 DeepSeek / Qwen / Kimi)。
- ✗ 只会背 Prompt 技巧,讲不清生成原理 / token / 采样。
- ✗ 项目只有功能描述,没有**指标、踩坑、决策依据**。
- ✗ 张口「用 RAG 解决所有问题」,却不懂切块、召回、重排、评测。
- ✗ 对**成本、延迟、安全性**这些工程现实一问三不知。
- ✗ 数据/参数记忆错误还硬拗 — 答「据公开估计,以官方为准」反而加分。
- ✗ 问「会不会微调」答「会」,却讲不清 LoRA 与适用边界。

## 加分项(主动展示)
- ✓ 主动聊 MCP / Agent 生态与当周行业动态(面试官判断「是不是真在用」)。
- ✓ 项目有量化产出:「问答命中率 60%→85%」「推理成本降 50%」「TTFT 从 X 降到 Y」。
- ✓ 能讲一次「踩坑 → 排查 → 解决」完整过程(检索不准/幻觉/截断/注入)。
- ✓ 有对比决策意识:何时 RAG、何时微调、何时换模型、何时私有化。
- ✓ 动手证据:本地部署过开源模型、写过 Agent、微调过模型、跑过压测。
- ✓ 坦诚边界:承认公开基准与业务会偏离,主动补「自有评估集」。

## 面试前自我检查清单
- [ ] 能现场画出 Decoder-only 前向流程(输入→嵌入→注意力→FFN→概率)。
- [ ] 能讲清 KV Cache 为什么决定推理性能。
- [ ] 能分清 RAG / 微调 / 提示工程的适用边界并举例。
- [ ] 能给「企业知识库问答」完整系统设计(含检索、权限、评估)。
- [ ] 能讲清 MCP 与工具调用,以及 Agent 的安全性设计。
- [ ] 知道至少 6 个模型(海外+国内)及其定位/特点。
- [ ] 有至少一个「能跑 + 有指标 + 谈得出踩坑」的自建项目。

---

# 附录 面试前速记卡

> 面试前一天背这三张卡,配合第 12 章题库即可上场。

## 卡一:大模型是什么(30 秒版)
**一句话**:海量文本 + Transformer + 「预测下一个 token」+ 规模涌现。
- 常见训练阶段:**预训练(学习通用语言与知识表示)→ SFT(调整指令遵循和输出格式)→ 对齐 RLHF/DPO(优化偏好与安全)**;推理模型可能再加「长 CoT + RL with 可验证奖励」。各阶段都会影响知识、能力和行为，只是侧重点不同。
- 核心直觉:模型是「上下文建模器」,输出按概率采样 → 所以会幻觉、会越狱。

## 卡二:企业落地三件套
- **RAG** = 让模型「有依据、可溯源」(检索 → 精排 → 带出处生成)。
- **Function Calling / MCP** = 通过工具调用或统一协议让模型「能行动、能接外部世界」。
- **Agent** = 让模型「能办成一件事」(规划 + 工具 + 记忆 + 反馈循环)。
- 决策口诀:**动态、私有、需要出处的知识→优先 RAG;固定格式/风格→考虑微调;需要外部能力与执行→工具+Agent;别轻易训练模型。**

## 卡三:谈应用必带数据与证据
- 用什么模型、什么成本、多少延迟、效果指标(召回率/命中率/TTFT/降本 %)、踩过什么坑、如何兜底与合规。
- 「数据+证据」的谈法,是面试官判断「你真做过 vs 只会背书」的关键。

---

# 修订记录与数据说明

## 修订记录
- **v1(初版)**:基础概念 + 技术原理 + 生态应用 + 24 题题库。
- **v2(本版)**:扩充为 15 章 + 附录;新增训练对齐、推理部署、提示词、RAG、Agent 深度章节;面试题库升级为 6 类带追问;学习路线细化到周;统一核对并修正了数据/概念细节与格式笔误。

## 数据来源与准确性声明
- 行业信息应以厂商公告、官方模型卡、技术报告和产品文档为主；百科和社区资料适合了解背景，不应作为参数、价格或评测结论的唯一来源。
- 文档只保留少量有明确公开依据的示例；模型参数、上下文窗口、价格和评测成绩均可能随版本和口径变化，使用前请核对**对应版本的官方资料**。
- **概念/公式性内容**(Transformer、注意力、KV Cache、RoPE、LoRA、DPO、Scaling、6ND、Chinchilla 的计算最优结论等)应理解为常用模型或近似，具体实现和训练配方可能不同。
- 行业信息具有时效性;**最新模型、定价与能力请以各厂商官网为准**,面试前建议做一次刷新。
